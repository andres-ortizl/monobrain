# monobrain · ui

Web SPA — the shared-plane brain UI: browse memory, the proposal-approval queue,
curator/brain activity, skills. A client of the `core` OpenAPI. Distinct from the
`specdex` companion's *local* fleet view.

Not built. Framework TBD.
