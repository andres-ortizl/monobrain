# monobrain · dex

Rust CLI — the only thing coding agents touch: emit events (spool + retry), retrieve
memory (`find` / `ls` / `read`), and the client-side staleness check (`git diff` locally
→ reports stale flags). Codegen'd from the `core` OpenAPI.

Not built.
